node_modules/
.DS_Store
Thumbs.db
*.log
dist/
.cache/

# Env — allow only .example variants
.env
.env.*
!.env.example
!.env.*.example

# Secrets & credentials — never commit (everything here is public via GH Pages)
auth/
secrets.json
credentials.json
service-account*.json
*.pem
*.key
.dev.vars
.secrets/

# Large media — host externally or use Git LFS
Video/

# Claude / local tooling
.claude/
